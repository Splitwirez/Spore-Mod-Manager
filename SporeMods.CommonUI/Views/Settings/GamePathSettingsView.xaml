<UserControl x:Class="SporeMods.Views.GamePathSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cui="clr-namespace:SporeMods.CommonUI"
             xmlns:e="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:v="clr-namespace:SporeMods.Views"
             xmlns:vm="clr-namespace:SporeMods.ViewModels"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:GamePathSettingsViewModel/>
    </UserControl.DataContext>
    <GroupBox Header="{DynamicResource Settings!Folders!Header}" IsEnabled="False">
        <cui:StackPanelEx Orientation="Vertical" Spacing="{DynamicResource SettingsControlsVerticalSpacing}">
            <TextBlock Text="Soon:tm: (PLACEHOLDER) (NOT LOCALIZED)"/>
            <v:GamePathView DataContext="{Binding GaData}" Visibility="Collapsed"/>
            <v:GamePathView DataContext="{Binding SporebinEp1}" Visibility="Collapsed"/>
            <v:GamePathView DataContext="{Binding CoreData}" Visibility="Collapsed"/>
        </cui:StackPanelEx>
    </GroupBox>
</UserControl>
